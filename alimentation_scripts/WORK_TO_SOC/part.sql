LOGON localhost/dbc,dbc;

INSERT INTO SOC.R_PART (
    PART_ID, 
    SRC_ID, 
    SRC_TYP
)
SELECT
    PART_ID,
    INDIVIDU_ID,
    FONCTION_INDIVIDU
FROM WRK.WRK_RPART

.IF ERRORCODE <> 0 THEN .QUIT 100;

.LOGOFF;
.EXIT;
